<?php

// This is an example settings file to investigate how settings can be overridden for different environments.

if($_ENV['AH_SITE_ENVIRONMENT'] == "dev") {
	//$config['system.site']['name'] = 'Rich Settings and Dev';
}

//$config['system.site']['name'] = 'Rich Settings';
